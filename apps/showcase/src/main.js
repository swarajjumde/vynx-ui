// Deprecated entry: the Vue Lynx showcase now boots from `src/index.js`
// (matching the official `create-vue-lynx` scaffold). Kept as a thin
// re-export for any older references; prefer importing `./index.js`.
export { default } from './index.js';
