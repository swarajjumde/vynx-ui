# Task 000: Orchestration Handshake

## Scope

1. Read `CLAUDE.md`.
2. Create `.ai/handshake/claude-ok.txt`.
3. Put exactly `CLAUDE_ORCHESTRATION_OK` in the file.
4. Modify nothing else.
5. Return a short changed-files summary.

## Constraints

- Do not read secrets, `.env`, `.env.*`, credential directories, browser data, or authentication storage.
- Do not run `git push`, `npm publish`, destructive commands, downloads, or remote scripts.
