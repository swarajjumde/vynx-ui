# JavaScript Consumer Standard

- Consumer docs and examples are JavaScript-first.
- Packages must expose compiled JavaScript and type declarations when TypeScript is used internally.
- Avoid requiring consumers to write TypeScript, configure path aliases, or import private files.
- Examples should show ordinary Vue usage from JavaScript entry files and SFCs.
- Treat TypeScript types as editor help, not as the only documentation of runtime behaviour.
