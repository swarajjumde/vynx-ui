# Styling Standard

- Component styling must use design tokens for colour, spacing, radius, typography, and shadow.
- Avoid hardcoded component colours except inside token definitions.
- Support light and dark modes through token values.
- Themes must be original and must not copy another product's visual identity.
- Keep component styles Lynx-compatible and avoid browser DOM assumptions.
